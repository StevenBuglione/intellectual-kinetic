# LyX Capability Parity Matrix

## Feature Family: document classes and document settings

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| docclass-select | Select document class | document classes and document settings | User chooses a document class such as article, book, report, letter, presentation, or class-specific template family that governs available styles, modules, and export defaults. | `.ref/lyx/lib/templates/Articles/`, `.ref/lyx/lib/templates/Books/`, `.ref/lyx/lib/doc/Tutorial.lyx`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/BufferParams.cpp`, `.ref/lyx/lib/layouts/` | core parity | Document class selection is foundational because it constrains valid structure, available environments, and export defaults across the whole document. | document settings surface, multi-surface workflow | document metadata, structural node | serializer mapping, preamble/package dependency, round-trip/parity risk | foundational | Core entry point for later class-specific adaptation. |
| module-enable-disable | Enable and disable document modules | document classes and document settings | User toggles shipped modules that add environments, insets, style families, or export-sensitive behaviors to the current document. | `.ref/lyx/lib/doc/Customization.lyx`, `.ref/lyx/lib/examples/Modules/`, `.ref/lyx/lib/layouts/*.module` | `.ref/lyx/src/BufferParams.cpp` | core parity | Module selection materially changes document semantics and available authoring surfaces, so it must be modeled as a first-class capability. | document settings surface, multi-surface workflow | document metadata, structural node | preamble/package dependency, serializer mapping | foundational | Specialized modules still vary in later classification, but the selection surface itself is core. |
| doc-metadata-settings | Configure document metadata | document classes and document settings | User sets title, author, date, affiliation, and other document-level metadata that affect both editing surfaces and exported output. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/templates/Articles/`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/BufferParams.cpp`, `.ref/lyx/src/Author.cpp` | core parity | Metadata is a core structured-authoring surface with direct export implications and cross-template importance. | document settings surface, toolbar/sidebar/dialog surface | document metadata | serializer mapping, preamble/package dependency | foundational | Some layout-defined metadata fields differ by class and may need later adaptation. |
| custom-preamble-edit | Edit custom LaTeX preamble | document classes and document settings | User adds custom preamble content and package directives to the document settings for export and compile control. | `.ref/lyx/lib/doc/LaTeXConfig.lyx`, `.ref/lyx/lib/doc/Customization.lyx` | `.ref/lyx/src/BufferParams.cpp` | adapted parity | The capability is central for advanced authoring, but the final product may need stronger validation and safer UX than LyX's raw-text preamble model. | document settings surface, toolbar/sidebar/dialog surface | export-only metadata | preamble/package dependency, compile behavior, round-trip/parity risk | early | Preserve intent while likely constraining or structuring freeform preamble editing. |
| master-document-linking | Link master and child documents | document classes and document settings | User links a child document into a larger master-document workflow for multi-file projects. | `.ref/lyx/lib/templates/Books/`, `.ref/lyx/lib/examples/Books/` | `.ref/lyx/src/Buffer.cpp` | deferred parity | The capability is real and user-visible, but it adds project-scale coordination complexity beyond the first semantic core. | multi-surface workflow, command | document metadata, structural node | serializer mapping, round-trip/parity risk | late | Export precedence and include semantics still need deeper proof. |

## Feature Family: document structure and semantic blocks

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| sectioning-hierarchy | Create section hierarchy | document structure and semantic blocks | User inserts and restructures parts, chapters, sections, subsections, and related hierarchy levels appropriate to the chosen class. | `.ref/lyx/lib/doc/Tutorial.lyx`, `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/templates/Books/`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/Buffer.cpp` | core parity | Hierarchical sectioning is a primary structured-authoring capability with direct document-semantic and export implications. | command, toolbar/sidebar/dialog surface | structural node, block node | serializer mapping | foundational | Class-specific depth differences should remain notes, not separate rows, unless parity treatment diverges materially. |
| abstract-block | Insert abstract block | document structure and semantic blocks | User inserts an abstract-style semantic block whose exact placement and rendering depends on the document class. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/layouts/`, `.ref/lyx/lib/templates/Articles/` | `.ref/lyx/src/Buffer.cpp` | core parity | Abstract handling is common, user-visible, and semantically meaningful across article-like document flows. | command, node | structural node, document metadata | serializer mapping | foundational | Per-class behavior variation is captured in notes rather than split into many rows. |
| titlepage-and-frontmatter | Use title pages and front-matter blocks | document structure and semantic blocks | User works with title pages, dedications, prefaces, acknowledgements, and front-matter/back-matter structures where supported by class or template. | `.ref/lyx/lib/templates/Books/`, `.ref/lyx/lib/examples/Books/`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/Buffer.cpp` | adapted parity | These are meaningful semantic blocks but often vary heavily by class, so parity will likely adapt the UI while preserving semantics. | multi-surface workflow, command | structural node, document metadata | serializer mapping, preamble/package dependency | early | Class-specific front-matter blocks may need later subtype notes. |
| quotation-verse-blocks | Insert quotation, quote, and verse-like semantic blocks | document structure and semantic blocks | User inserts semantically distinct quoted or verse blocks that affect structure and rendering. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/Buffer.cpp` | core parity | These blocks are clearly user-visible and semantically distinct from generic paragraphs. | node, command | block node | serializer mapping | foundational | Kept as one row because parity treatment is similar even though style labels vary. |
| branch-conditional-content | Use branch-controlled conditional content | document structure and semantic blocks | User marks content under branch conditions and toggles branch visibility/export state. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/examples/`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/BranchList.cpp`, `.ref/lyx/lib/ui/stdmenus.inc` | adapted parity | Branches materially affect document semantics and export, but the final UX may differ in a server-side collaborative editor. | plugin/state management, multi-surface workflow | document metadata, structural node | serializer mapping, compile behavior, round-trip/parity risk | early | Export-state sensitivity makes this important despite likely adapted UX. |

## Feature Family: text formatting and inline styles

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| inline-emphasis-styles | Apply inline emphasis styles | text formatting and inline styles | User applies emphasis, strong emphasis, code-like inline styling, small caps, strikeout, and related inline styles that affect semantics or export. | `.ref/lyx/lib/doc/Tutorial.lyx`, `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/layouts/` | `.ref/lyx/lib/latexfonts`, `.ref/lyx/src/Color.cpp` | core parity | Inline styles are fundamental editing surfaces with direct serializer consequences. | mark, command | inline model | serializer mapping | foundational | Kept grouped where semantics remain close; materially different semantic inline constructs appear in later rows. |
| font-family-size-selection | Select font family, shape, and size | text formatting and inline styles | User changes font family, shape, and size at inline scope, affecting visual output and potentially package usage. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/doc/Customization.lyx`, `.ref/lyx/lib/latexfonts` | `.ref/lyx/src/Color.cpp`, `.ref/lyx/lib/latexfonts` | adapted parity | A core authoring surface, but some LyX font controls map to LaTeX more directly than a web-native editor typically would. | mark, toolbar/sidebar/dialog surface | inline model | serializer mapping, preamble/package dependency | foundational | Font mapping may need safer or more opinionated UX in the final product. |
| text-color-and-highlighting | Apply text color and highlighting | text formatting and inline styles | User colors text or applies visually distinct highlighting with export-sensitive color output. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/examples/`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/Color.cpp`, `.ref/lyx/lib/latexcolors` | adapted parity | User-visible and semantically relevant in some workflows, but color scope and package behavior make direct parity slightly more complex. | mark, command | inline model | serializer mapping, preamble/package dependency | early | Export tests indicate color handling deserves careful treatment. |
| change-tracking | Track changes with author attribution | text formatting and inline styles | User records insertions, deletions, and author-attributed tracked changes in the document. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/ui/stdmenus.inc` | `.ref/lyx/src/Changes.cpp`, `.ref/lyx/src/Author.cpp` | adapted parity | Clearly shipped and high-value, but collaborative/server-side workflows may demand an adapted model rather than LyX's desktop-centric behavior. | mark, plugin/state management, multi-surface workflow | inline model, document metadata | serializer mapping, round-trip/parity risk | mid | Preserve author-attributed change semantics even if UX changes. |

## Feature Family: math and theorem-like structures

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| inline-and-display-math | Insert inline and display math | math and theorem-like structures | User inserts inline formulas and display formulas with structured math editing instead of plain text TeX entry. | `.ref/lyx/lib/doc/Math.lyx`, `.ref/lyx/lib/examples/`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/mathed/`, `.ref/lyx/lib/symbols` | core parity | Structured math is central to LyX's value proposition and heavily constrains AST and serializer design. | node, command, toolbar/sidebar/dialog surface | math model | serializer mapping, preview behavior | foundational | High-priority parity area. |
| equation-numbering-and-labels | Number equations and reference them | math and theorem-like structures | User numbers equations, suppresses numbers when needed, and connects equations to label/reference workflows. | `.ref/lyx/lib/doc/Formula-numbering.lyx`, `.ref/lyx/lib/doc/Math.lyx` | `.ref/lyx/src/mathed/`, `.ref/lyx/src/insets/` | core parity | Equation numbering interacts directly with cross-reference semantics and LaTeX export fidelity. | command, node | math model, reference/citation model | serializer mapping, compile behavior | foundational | Strong dependency with cross-reference parity. |
| math-structures-matrices-alignment | Use matrices, aligned layouts, arrays, and multi-line math structures | math and theorem-like structures | User builds structured matrix and alignment layouts with semantics beyond flat TeX text. | `.ref/lyx/lib/doc/Math.lyx`, `.ref/lyx/lib/examples/` | `.ref/lyx/src/mathed/InsetMath*` | core parity | These are common, semantically distinct math constructs that materially shape both AST and serializer design. | node, command | math model | serializer mapping, preview behavior | foundational | Covered as one row because the parity treatment stays within structured math layout modeling. |
| theorem-like-environments | Insert theorem-like semantic environments | math and theorem-like structures | User inserts theorem, lemma, proof, corollary, definition, and related theorem-like blocks with numbering/styling behavior. | `.ref/lyx/lib/layouts/theorems-ams.module`, `.ref/lyx/lib/layouts/theorems-starred.module`, `.ref/lyx/lib/examples/` | `.ref/lyx/src/Buffer.cpp` | core parity | Theorem-like structures are user-visible semantic blocks with numbering and export implications, even if individual labels are best treated as variants of one capability. | node, command | structural node, block node | serializer mapping, preamble/package dependency | early | Individual theorem labels remain subtype notes unless later parity treatment diverges. |
| math-macros-and-decorations | Use math macros, accents, decorations, and symbol families | math and theorem-like structures | User applies structured math accents, decorations, macros, and symbol-driven constructs that affect both editing and rendering. | `.ref/lyx/lib/doc/Math.lyx`, `.ref/lyx/lib/examples/` | `.ref/lyx/src/mathed/`, `.ref/lyx/lib/symbols` | core parity | These behaviors are integral to real math authoring and not safely reducible to plain-text macros. | node, command | math model | serializer mapping, preview behavior | early | Some specialized macro workflows may later become adapted parity. |
| math-extern-cas | Use math extern / CAS integration | math and theorem-like structures | User connects math expressions to external CAS tools such as Octave, Maxima, Mathematica, or Maple. | `.ref/lyx/lib/ui/stdmenus.inc`, `.ref/lyx/lib/doc/Additional.lyx` | `.ref/lyx/src/mathed/` | deferred parity | Clearly shipped and user-visible, but toolchain dependency and narrower audience make it a later parity target. | multi-surface workflow, command | math model, export-only metadata | serializer mapping, round-trip/parity risk | late | External-tool integration should not block baseline structured-math parity. |

## Feature Family: tables

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| table-insert-edit | Insert and edit structured tables | tables | User creates tables with rows, columns, alignment, borders, and cell-level editing behaviors. | `.ref/lyx/lib/doc/EmbeddedObjects.lyx`, `.ref/lyx/lib/examples/Graphics_and_Insets/`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/insets/InsetTabular.cpp` | core parity | Structured tables are a major authoring surface with heavy AST and serializer implications. | node, toolbar/sidebar/dialog surface | structural node, block node | serializer mapping | early | High-priority for canonical table modeling. |
| table-cell-merge-and-span | Merge cells and use row/column spans | tables | User merges cells and creates multicolumn or multirow table structures with output-sensitive semantics. | `.ref/lyx/lib/doc/EmbeddedObjects.lyx`, `.ref/lyx/autotests/export/` | `.ref/lyx/src/insets/InsetTabular.cpp` | core parity | Merge/span semantics materially affect document structure, export, and later render fidelity. | node, command | structural node, block node | serializer mapping, round-trip/parity risk | early | Full edge semantics still need deeper proof and remain an open question. |
| longtable-mode | Enable multi-page long tables | tables | User creates long tables that can span pages using longtable-style export behavior. | `.ref/lyx/lib/examples/`, `.ref/lyx/lib/layouts/`, `.ref/lyx/autotests/export/` | `.ref/lyx/src/insets/InsetTabular.cpp` | adapted parity | Important export-sensitive table behavior, but may need a more explicit surface than LyX's original UI. | document settings surface, node | block node, export-only metadata | preamble/package dependency, serializer mapping | mid | Strong LaTeX implications; may not need to be first-wave editor UX. |
| formal-booktabs-style | Apply formal / booktabs table styling | tables | User applies professional table styling such as booktabs-like horizontal rules and formal presentation settings. | `.ref/lyx/lib/examples/`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/insets/InsetTabular.cpp` | adapted parity | Clearly user-visible and export-sensitive, but stylistic configuration likely rides on top of core table semantics. | toolbar/sidebar/dialog surface | export-only metadata, block node | preamble/package dependency, serializer mapping | mid | Best handled after baseline table semantics are stable. |

## Feature Family: figures, images, floats, and captions

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| graphics-insert-configure | Insert and configure graphics | figures, images, floats, and captions | User inserts graphics and controls scale, rotation, file path, and related image-display settings. | `.ref/lyx/lib/doc/EmbeddedObjects.lyx`, `.ref/lyx/lib/examples/Graphics_and_Insets/` | `.ref/lyx/src/graphics/`, `.ref/lyx/lib/scripts/convertDefault.py` | core parity | Graphics insertion is common and materially impacts export, preview, and asset handling. | node, toolbar/sidebar/dialog surface | block node, export-only metadata | serializer mapping, preview behavior | early | Asset pipeline and converter behavior are important supporting concerns. |
| figure-and-table-floats | Use figure and table floats | figures, images, floats, and captions | User wraps graphics or tabular content in float containers with placement and numbering behavior. | `.ref/lyx/lib/doc/EmbeddedObjects.lyx`, `.ref/lyx/lib/layouts/`, `.ref/lyx/lib/examples/` | `.ref/lyx/src/Floating.cpp`, `.ref/lyx/src/FloatList.h` | core parity | Float containers materially affect semantics, numbering, placement, and later serializer design. | node, command | block node, structural node | serializer mapping, compile behavior | early | Foundational for academic publishing flows. |
| caption-management | Insert captions and short captions | figures, images, floats, and captions | User manages captions, optional short captions, and caption presentation for floats and related objects. | `.ref/lyx/lib/doc/EmbeddedObjects.lyx`, `.ref/lyx/lib/examples/`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/insets/`, `.ref/lyx/src/Floating.cpp` | core parity | Captions are tightly coupled to references, float numbering, and export semantics. | node, toolbar/sidebar/dialog surface | block node, reference/citation model | serializer mapping | early | Includes continued/short-caption behavior pressure in notes. |
| wrap-margin-floats | Use wrap, side, or margin float variants | figures, images, floats, and captions | User applies layout- or class-specific float variants such as wrap floats, side captions, or margin figures. | `.ref/lyx/lib/layouts/`, `.ref/lyx/lib/templates/Articles/`, `.ref/lyx/lib/examples/` | `.ref/lyx/src/Floating.cpp` | adapted parity | Clearly shipped and semantically meaningful, but highly class- and layout-specific, so better modeled after baseline float parity. | node, toolbar/sidebar/dialog surface | block node, export-only metadata | serializer mapping, preamble/package dependency | mid | Margin and wrap float granularity remains a follow-up question. |

## Feature Family: references, labels, and cross-references

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| label-insert | Insert labels on semantic targets | references, labels, and cross-references | User inserts labels on sections, figures, tables, equations, and related semantic targets. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/doc/Formula-numbering.lyx`, `.ref/lyx/lib/examples/` | `.ref/lyx/src/insets/`, `.ref/lyx/lib/ui/stdmenus.inc` | core parity | Labels are the foundation for cross-reference behavior and tightly coupled to document semantics. | command, toolbar/sidebar/dialog surface | reference/citation model | serializer mapping, compile behavior | foundational | Core dependency for equation and float reference parity. |
| cross-reference-insert | Insert cross-references to labeled objects | references, labels, and cross-references | User inserts cross-references to labeled sections, figures, tables, equations, and related targets. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/examples/`, `.ref/lyx/autotests/export/` | `.ref/lyx/src/insets/`, `.ref/lyx/lib/ui/stdmenus.inc` | core parity | This is a central structured-authoring capability with direct semantic and export consequences. | command, toolbar/sidebar/dialog surface, mark | reference/citation model | serializer mapping, compile behavior, preview behavior | foundational | Target-type differences remain notes unless later parity treatment diverges. |
| table-of-contents-and-lists | Generate table of contents and related generated lists | references, labels, and cross-references | User inserts generated navigational lists such as a table of contents and class-dependent lists of figures or tables. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/templates/Books/` | `.ref/lyx/src/Buffer.cpp` | core parity | Generated navigational structures are user-visible and depend directly on document semantics and labels. | command, node | structural node, document metadata | serializer mapping | early | Scope may expand later to class-specific generated lists. |

## Feature Family: citations and bibliography

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| bibliography-insert-manage | Insert and manage bibliography resources | citations and bibliography | User associates bibliography databases/resources with a document and prepares bibliography output. | `.ref/lyx/lib/doc/LaTeXConfig.lyx`, `.ref/lyx/lib/templates/Articles/`, `.ref/lyx/lib/examples/` | `.ref/lyx/src/BiblioInfo.cpp`, `.ref/lyx/src/CiteEnginesList.cpp` | core parity | Bibliography resources are central to scholarly authoring and strongly shape export behavior. | toolbar/sidebar/dialog surface, document settings surface | reference/citation model, document metadata | serializer mapping, preamble/package dependency | foundational | Multiple database and engine-selection details remain follow-up questions. |
| citation-insert-style-variants | Insert citations with style variants | citations and bibliography | User inserts citations and chooses among citation-command variants appropriate to the configured bibliography engine. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/doc/LaTeXConfig.lyx`, `.ref/lyx/lib/examples/`, `.ref/lyx/autotests/export/` | `.ref/lyx/src/BiblioInfo.cpp`, `.ref/lyx/src/CiteEnginesList.cpp` | core parity | Citation insertion is a high-value semantic authoring surface with direct AST and serializer implications. | command, toolbar/sidebar/dialog surface, mark | reference/citation model | serializer mapping, compile behavior | foundational | Natbib vs. biblatex differences stay in notes until later parity specs. |
| bibliography-output-control | Control bibliography formatting and output placement | citations and bibliography | User configures how bibliography output appears within the document and export flow. | `.ref/lyx/lib/doc/LaTeXConfig.lyx`, `.ref/lyx/lib/templates/Articles/` | `.ref/lyx/src/BiblioInfo.cpp` | adapted parity | Bibliography output control is important but often rides atop a stable citation model and may need product-specific simplification. | document settings surface, command | reference/citation model, export-only metadata | serializer mapping, preamble/package dependency | early | Exact engine-specific knobs may later be narrowed. |

## Feature Family: index, glossary, and nomenclature features

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| index-entry-insert | Insert index entries | index, glossary, and nomenclature features | User marks text or locations for index generation. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/examples/`, `.ref/lyx/lib/templates/Books/` | `.ref/lyx/src/insets/`, `.ref/lyx/lib/ui/stdmenus.inc` | adapted parity | Clearly user-visible and semantically meaningful, but external toolchain and later output processing make it a secondary parity target. | command, mark | reference/citation model, export-only metadata | serializer mapping, preamble/package dependency | mid | Core marking behavior matters even if final output generation is later or adapted. |
| nomenclature-and-glossary | Insert nomenclature or glossary-like entries | index, glossary, and nomenclature features | User creates glossary/nomenclature style entries for generated reference sections. | `.ref/lyx/lib/templates/Books/Springer_Monographs/09_Glossary.lyx`, `.ref/lyx/lib/doc/UserGuide.lyx` | `.ref/lyx/src/insets/` | adapted parity | Real and useful, but not foundational to the earliest structured-authoring core. | command, mark | reference/citation model, export-only metadata | serializer mapping, preamble/package dependency | mid | Exact scope and tooling remain partially unresolved. |
| index-property-macros | Use index-property variants such as subentry, sort key, see, and see also | index, glossary, and nomenclature features | User adds richer index semantics beyond a simple index mark. | `.ref/lyx/lib/ui/stdmenus.inc`, `.ref/lyx/lib/examples/` | `.ref/lyx/src/insets/` | deferred parity | Distinctly user-visible and semantically meaningful, but secondary to baseline index entry support. | toolbar/sidebar/dialog surface, mark | reference/citation model, export-only metadata | serializer mapping | late | Keep separate because these options materially change index semantics. |

## Feature Family: page, layout, and flow control

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| page-breaks-and-flow-breaks | Insert page and flow breaks | page, layout, and flow control | User inserts page breaks, clear-page style breaks, and related flow-control markers that affect output pagination or structure. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/examples/`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/insets/` | core parity | Flow-control markers directly affect output semantics and are common in longer documents. | command, node | block node, export-only metadata | serializer mapping, compile behavior | early | Important for long-form structured authoring. |
| multicolumn-and-landscape-modes | Apply multicolumn or landscape layout modes | page, layout, and flow control | User applies multi-column and landscape-oriented document or section behavior where supported by modules/classes. | `.ref/lyx/lib/layouts/multicol.module`, `.ref/lyx/lib/layouts/landscape.module`, `.ref/lyx/lib/examples/` | `.ref/lyx/src/BufferParams.cpp` | adapted parity | Layout-flow control is real and export-sensitive, but likely belongs after baseline document structure and floats are stable. | document settings surface, node | block node, export-only metadata | preamble/package dependency, serializer mapping | mid | Nesting and local-vs-global behavior remain partially unresolved. |
| headers-footers-page-style | Configure headers, footers, and page-style behavior | page, layout, and flow control | User configures page-style surfaces such as headers, footers, and class-specific running information. | `.ref/lyx/lib/templates/Books/`, `.ref/lyx/lib/layouts/`, `.ref/lyx/lib/doc/Customization.lyx` | `.ref/lyx/src/BufferParams.cpp` | adapted parity | User-visible and important for long-form publishing, but not required to establish the earliest semantic authoring core. | document settings surface, multi-surface workflow | document metadata, export-only metadata | serializer mapping, preamble/package dependency | mid | Page-style behavior is often class-specific. |
| spacing-margins-layout-options | Configure spacing, margins, and layout options | page, layout, and flow control | User adjusts spacing and layout-related document settings that affect output appearance and package selection. | `.ref/lyx/lib/doc/Customization.lyx`, `.ref/lyx/lib/doc/LaTeXConfig.lyx`, `.ref/lyx/lib/layouts/` | `.ref/lyx/src/BufferParams.cpp` | adapted parity | Important and clearly shipped, but often better represented as structured document settings rather than replicated desktop dialogs. | document settings surface, toolbar/sidebar/dialog surface | export-only metadata, document metadata | preamble/package dependency, serializer mapping | mid | Better treated as structured settings than raw dialog parity. |

## Feature Family: language, encoding, and multilingual text features

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| multilingual-language-selection | Apply document and paragraph language selection | language, encoding, and multilingual text features | User assigns document-level and local language settings that affect hyphenation, typography, and export package selection. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/examples/Language_Support/`, `.ref/lyx/lib/languages`, `.ref/lyx/autotests/export/latex/languages/` | `.ref/lyx/lib/languages`, `.ref/lyx/src/BufferEncodings.cpp` | core parity | Multilingual support is central to structured document semantics and directly shapes export and compile behavior. | toolbar/sidebar/dialog surface, mark | document metadata, inline model | preamble/package dependency, compile behavior, serializer mapping | early | Babel vs. Polyglossia logic remains an explicit blocking open question. |
| encoding-selection-validation | Select and validate character/font encodings | language, encoding, and multilingual text features | User chooses or relies on character/font encoding behavior that affects glyph availability and compile success. | `.ref/lyx/lib/doc/LaTeXConfig.lyx`, `.ref/lyx/autotests/export/latex/inputenc-*`, `.ref/lyx/autotests/export/latex/unicodesymbols/` | `.ref/lyx/src/BufferEncodings.cpp`, `.ref/lyx/lib/encodings`, `.ref/lyx/lib/latexfonts` | core parity | Encoding correctness is required for robust export, especially across multilingual and symbol-heavy documents. | document settings surface | document metadata, export-only metadata | compile behavior, preamble/package dependency | early | Encoding mismatches cause hard failures in the export evidence. |
| cjk-and-rtl-support | Use CJK and right-to-left language support | language, encoding, and multilingual text features | User authors in CJK or RTL languages with engine/package-sensitive rendering behavior. | `.ref/lyx/lib/examples/Language_Support/`, `.ref/lyx/autotests/export/latex/CJK/`, `.ref/lyx/autotests/export/latex/languages/` | `.ref/lyx/lib/languages`, `.ref/lyx/src/BufferEncodings.cpp` | deferred parity | Shipped and important, but the export evidence shows engine-specific and interaction-heavy complexity that should not block the initial parity core. | plugin/state management, document settings surface | inline model, document metadata | compile behavior, preamble/package dependency, round-trip/parity risk | late | Exact core-vs-engine-specific scope remains a blocking classification question. |
| multilingual-captions-and-local-overrides | Use multilingual captions or local language overrides in structured content | language, encoding, and multilingual text features | User applies language-sensitive behavior to captions or structured regions beyond a single document-default language. | `.ref/lyx/lib/examples/Modules/Multilingual_Captions.lyx`, `.ref/lyx/autotests/export/latex/languages/` | `.ref/lyx/lib/languages` | adapted parity | Real and export-sensitive, but best layered after the baseline multilingual model is stable. | toolbar/sidebar/dialog surface, mark | inline model, document metadata | serializer mapping, preamble/package dependency | mid | Relationship to the general language model remains a follow-up question. |

## Feature Family: export and rendering behavior

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| latex-export | Export structured documents to LaTeX | export and rendering behavior | User exports the document to LaTeX with class, package, encoding, and semantic structure preserved. | `.ref/lyx/lib/doc/LaTeXConfig.lyx`, `.ref/lyx/autotests/export/latex/`, `.ref/lyx/autotests/checklatexexports/` | `.ref/lyx/src/Exporter.cpp`, `.ref/lyx/src/Converter.cpp` | core parity | Deterministic LaTeX export is one of the primary downstream goals of this entire parity effort. | command, multi-surface workflow | export-only metadata, structural node | serializer mapping, compile behavior, round-trip/parity risk | foundational | Central downstream dependency for later render architecture work. |
| preview-and-compile-loop | Generate previews and compiled output | export and rendering behavior | User benefits from a preview/compile loop for math, graphics, and document output. | `.ref/lyx/lib/doc/Math.lyx`, `.ref/lyx/lib/doc/EmbeddedObjects.lyx`, `.ref/lyx/lib/scripts/lyxpreview2bitmap.py` | `.ref/lyx/lib/scripts/lyxpreview2bitmap.py`, `.ref/lyx/lib/scripts/lyxpreview_tools.py` | core parity | Live render and preview behavior is a core project goal and must be fed by the same semantic export model. | command, plugin/state management, multi-surface workflow | export-only metadata | preview behavior, compile behavior, diagnostics mapping | early | Preview renderer fallback and determinism still need downstream specification. |
| docbook-export | Export to DocBook | export and rendering behavior | User exports structured documents to DocBook XML with semantic mapping preserved as much as possible. | `.ref/lyx/autotests/export/docbook/`, `.ref/lyx/lib/doc/Additional.lyx` | `.ref/lyx/src/Exporter.cpp`, `.ref/lyx/src/Converter.cpp` | adapted parity | This is clearly shipped and semantically rich, but not part of the first core parity stack for the target product. | command | structural node, export-only metadata | serializer mapping, round-trip/parity risk | mid | Useful as a semantic-checking reference even if not an MVP surface. |
| xhtml-export | Export to XHTML/CSS | export and rendering behavior | User exports documents to XHTML with CSS-sensitive output behavior. | `.ref/lyx/autotests/export/xhtml/`, `.ref/lyx/lib/doc/Additional.lyx` | `.ref/lyx/src/Exporter.cpp`, `.ref/lyx/src/Converter.cpp` | adapted parity | Real and valuable, but the downstream product's render stack will likely choose a different web representation path. | command | export-only metadata, structural node | serializer mapping, preview behavior | mid | Math rendering strategy for XHTML remains an open question. |
| export-robustness-and-recovery | Preserve export robustness for malformed or edge-case structures | export and rendering behavior | User expects export to survive or meaningfully report malformed structures, missing packages, and known edge cases. | `.ref/lyx/autotests/export/latex/lyxbugs-resolved/`, `.ref/lyx/autotests/checklatexexports/` | `.ref/lyx/src/Exporter.cpp`, `.ref/lyx/src/Converter.cpp` | core parity | Export robustness directly affects product trustworthiness and live-render experience. | plugin/state management, command | export-only metadata | compile behavior, diagnostics mapping, round-trip/parity risk | early | Fail-fast vs. graceful-degradation policy remains a blocking downstream design question. |

## Feature Family: templates, layouts, and customization surfaces

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| template-selection | Start from shipped templates | templates, layouts, and customization surfaces | User begins authoring from shipped article, book, presentation, letter, poster, CV, or specialized templates. | `.ref/lyx/lib/templates/`, `.ref/lyx/lib/examples/`, `.ref/lyx/lib/doc/Tutorial.lyx` | `.ref/lyx/lib/layouts/`, `.ref/lyx/src/BufferParams.cpp` | core parity | Templates are a major user-facing entry surface and strongly shape later document semantics. | multi-surface workflow, document settings surface | document metadata, structural node | serializer mapping, preamble/package dependency | early | Template-specific behavior may later force finer-grained adaptation. |
| custom-semantic-insets | Use layout-defined custom semantic insets | templates, layouts, and customization surfaces | User inserts layout-defined semantic insets such as affiliation, email, author role, code-like blocks, and other class/module-driven semantic elements. | `.ref/lyx/lib/layouts/*.layout`, `.ref/lyx/lib/examples/`, `.ref/lyx/lib/templates/` | `.ref/lyx/src/insets/`, `.ref/lyx/lib/layouts/` | adapted parity | Shipped and semantically meaningful, but too layout-driven to freeze prematurely into many separate parity commitments without further subtype analysis. | node, toolbar/sidebar/dialog surface | structural node, inline model | serializer mapping | mid | Flex-inset granularity remains a blocking open question. |
| customization-of-bindings-and-ui-surfaces | Customize bindings, toolbars, and UI-related authoring surfaces | templates, layouts, and customization surfaces | User changes authoring environment surfaces such as key bindings, toolbars, and related configurable behavior. | `.ref/lyx/lib/doc/Customization.lyx`, `.ref/lyx/lib/ui/` | `.ref/lyx/lib/ui/stdmenus.inc`, `.ref/lyx/src/` | deferred parity | Shipped and user-visible, but not required to establish semantic authoring or deterministic export parity. | toolbar/sidebar/dialog surface, command | none | none | late | Important for comfort and power-user workflows, not for the first semantic core. |

## Feature Family: editing workflow and user-facing control surfaces

| feature_id | capability | family | behavior | sources | supporting_sources | classification | classification_rationale | tiptap_impact | ast_impact | latex_render_impact | sequence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| undo-redo-history | Use undo and redo history | editing workflow and user-facing control surfaces | User replays and reverses document changes through an explicit undo/redo history model. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/ui/stdmenus.inc` | `.ref/lyx/src/Undo.cpp` | core parity | A reliable history model is mandatory for editing trust and downstream collaborative behavior. | plugin/state management, command | none | none | foundational | Core editing infrastructure with broad downstream implications. |
| find-replace | Search and replace document content | editing workflow and user-facing control surfaces | User searches for content and replaces matches with advanced or context-aware search behavior. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/ui/stdmenus.inc` | `.ref/lyx/src/lyxfind.cpp` | adapted parity | Important and clearly shipped, but not foundational to the first semantic parity layer. | command, toolbar/sidebar/dialog surface | none | none | mid | Can be layered on top of a stable document model. |
| spellcheck-and-thesaurus | Use spell-checking and thesaurus surfaces | editing workflow and user-facing control surfaces | User runs spell-check and thesaurus lookup against document text. | `.ref/lyx/lib/doc/UserGuide.lyx`, `.ref/lyx/lib/ui/stdmenus.inc` | `.ref/lyx/src/`, `.ref/lyx/lib/languages` | adapted parity | Useful and clearly shipped, but partly backend-dependent and not core to semantic authoring parity. | toolbar/sidebar/dialog surface, command | none | none | mid | Backend scope remains partially unresolved. |
| paste-special | Paste content through format-specific import paths | editing workflow and user-facing control surfaces | User pastes through format-specific variants such as HTML, LaTeX, PDF, PNG, JPEG, and related clipboard-driven paths. | `.ref/lyx/lib/ui/stdmenus.inc` | `.ref/lyx/src/` | adapted parity | User-visible and potentially high-value, but detection and conversion details remain partially unresolved. | command, toolbar/sidebar/dialog surface, multi-surface workflow | structural node, inline model | serializer mapping, round-trip/parity risk | mid | Clipboard detection logic remains a follow-up item. |
| multi-view-bookmarks-fullscreen | Use split views, bookmarks, and fullscreen workflow aids | editing workflow and user-facing control surfaces | User manages panes/views, bookmarks, and fullscreen workflow helpers during editing. | `.ref/lyx/lib/ui/stdmenus.inc`, `.ref/lyx/lib/doc/UserGuide.lyx` | `.ref/lyx/src/Cursor.h`, `.ref/lyx/src/DocIterator.h` | adapted parity | These workflow aids are clearly shipped, but they should be adapted to the final product shell rather than block semantic parity work. | multi-surface workflow, command | none | none | late | Important for workflow comfort, not for the first semantic core. |
| statistics-preferences-reconfigure | Use statistics, preferences, and reconfigure/admin surfaces | editing workflow and user-facing control surfaces | User opens document statistics, preferences, and reconfigure/admin commands that influence the authoring environment or external tool discovery. | `.ref/lyx/lib/ui/stdmenus.inc`, `.ref/lyx/lib/doc/Customization.lyx` | `.ref/lyx/src/`, `.ref/lyx/lib/scripts/` | deferred parity | Real and useful, but only indirectly tied to semantic authoring and export parity. | toolbar/sidebar/dialog surface, command | none | none | late | Reconfigure is particularly environment-sensitive. |
| compare-and-version-control | Compare documents and use version-control integration | editing workflow and user-facing control surfaces | User compares documents and invokes built-in version-control commands for tracked files. | `.ref/lyx/lib/ui/stdmenus.inc`, `.ref/lyx/lib/doc/UserGuide.lyx` | `.ref/lyx/src/Compare.cpp`, `.ref/lyx/src/` | deferred parity | Clearly shipped, but backend- and environment-dependent enough to defer until the core authoring stack is stable. | command, multi-surface workflow | none | none | late | VCS backend scope remains unresolved. |

## Supporting Internal Capabilities

| internal_feature_id | internal_capability | linked_primary_feature_ids | parity_relevance | sources | tiptap_impact | ast_impact | latex_render_impact | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| internal-undo-stack | Undo/redo stack | undo-redo-history | Deterministic history handling materially shapes editor state, command semantics, and future collaboration behavior. | `.ref/lyx/src/Undo.cpp` | plugin/state management | none | none | Core editing support mechanism. |
| internal-change-registry | Change and author registry | change-tracking | Change attribution and tracked-change rendering depend on author and change registries. | `.ref/lyx/src/Changes.cpp`, `.ref/lyx/src/Author.cpp` | plugin/state management | document metadata, inline model | serializer mapping | Supports tracked changes and author-attributed output. |
| internal-buffer-serialization | Buffer serialization and lyx2lyx format versioning | docclass-select, module-enable-disable, master-document-linking | Internal persistence/versioning constrains round-trip fidelity and document-structure persistence. | `.ref/lyx/src/Buffer.cpp`, `.ref/lyx/lib/lyx2lyx/` | none | structural node, document metadata | round-trip/parity risk | Important for understanding document persistence, even if not directly mirrored. |
| internal-bibliography-engine | Bibliography engine and cite engine registry | bibliography-insert-manage, citation-insert-style-variants, bibliography-output-control | Citation semantics and bibliography output depend on engine selection and bibliography parsing. | `.ref/lyx/src/BiblioInfo.cpp`, `.ref/lyx/src/CiteEnginesList.cpp` | toolbar/sidebar/dialog surface | reference/citation model | serializer mapping, preamble/package dependency | Material support for bibliography parity. |
| internal-graphics-converter-pipeline | Graphics conversion and asset pipeline | graphics-insert-configure, figure-and-table-floats | Image conversion, scaling, and external tool use materially affect output and preview behavior. | `.ref/lyx/src/graphics/`, `.ref/lyx/lib/scripts/convertDefault.py` | plugin/state management | export-only metadata | serializer mapping, preview behavior | Important for asset-processing parity. |
| internal-math-inset-hierarchy | Math inset hierarchy | inline-and-display-math, equation-numbering-and-labels, math-structures-matrices-alignment, math-macros-and-decorations, math-extern-cas | Structured math editing is implemented through a large inset hierarchy that materially affects AST and serializer completeness. | `.ref/lyx/src/mathed/`, `.ref/lyx/lib/symbols` | node | math model | serializer mapping, preview behavior | Core support mechanism for all math parity work. |
| internal-tabular-feature-system | Tabular feature system | table-insert-edit, table-cell-merge-and-span, longtable-mode, formal-booktabs-style | Tabular features encode merge/span, borders, alignment, and advanced table behavior with direct AST implications. | `.ref/lyx/src/insets/InsetTabular.cpp` | node | structural node, block node | serializer mapping, round-trip/parity risk | Critical support mechanism for table parity. |
| internal-float-registry | Float registry and placement support | figure-and-table-floats, wrap-margin-floats, caption-management | Float types and placement behavior are centrally registered and influence serializer behavior. | `.ref/lyx/src/Floating.cpp`, `.ref/lyx/src/FloatList.h` | none | export-only metadata, block node | serializer mapping, compile behavior | Supports class-specific and float-specific behavior. |
| internal-branch-registry | Branch registry | branch-conditional-content | Branch visibility and export state are driven by a branch registry and related runtime state. | `.ref/lyx/src/BranchList.cpp` | plugin/state management | document metadata | compile behavior, serializer mapping | Material support for conditional-content parity. |
| internal-language-registry | Language and encoding registries | multilingual-language-selection, encoding-selection-validation, cjk-and-rtl-support, multilingual-captions-and-local-overrides | Language/encoding data drives package selection, hyphenation, and compile-sensitive output. | `.ref/lyx/lib/languages`, `.ref/lyx/lib/encodings`, `.ref/lyx/src/BufferEncodings.cpp` | document settings surface | document metadata, inline model | preamble/package dependency, compile behavior | Core support mechanism for multilingual parity. |
| internal-export-converter-pipeline | Exporter and converter pipeline | latex-export, docbook-export, xhtml-export, export-robustness-and-recovery, preview-and-compile-loop | Export and preview behavior are mediated by converter dispatch, exporter logic, and external script orchestration. | `.ref/lyx/src/Exporter.cpp`, `.ref/lyx/src/Converter.cpp`, `.ref/lyx/lib/scripts/` | command | export-only metadata | serializer mapping, compile behavior, diagnostics mapping | Central support mechanism for downstream export/render architecture. |
| internal-layout-template-system | Layout and template registry | docclass-select, module-enable-disable, template-selection, custom-semantic-insets | Layouts, templates, and modules define many of the valid styles, insets, and class-specific semantic surfaces exposed to users. | `.ref/lyx/lib/layouts/`, `.ref/lyx/lib/templates/` | document settings surface | structural node, document metadata | serializer mapping, preamble/package dependency | Material support for class- and layout-driven parity. |
