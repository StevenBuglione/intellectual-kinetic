# LyX Capability Open Questions

| question_id | linked_feature_or_family | summary | unresolved_reason | sources | blocking_status | next_step |
| --- | --- | --- | --- | --- | --- | --- |
| oq-babel-polyglossia-selection | language, encoding, and multilingual text features | Babel vs. Polyglossia selection rules are not yet reduced to a precise parity algorithm. | Export tests show engine-sensitive behavior, but the exact auto-selection logic is not fully localized. | `.ref/lyx/autotests/export/latex/languages/`, `.ref/lyx/lib/doc/LaTeXConfig.lyx`, `.ref/lyx/src/BufferParams.cpp` | blocking | Localize engine/package selection logic in the runtime source and document the parity rule for later LaTeX/live-render design. |
| oq-cjk-classification | language, encoding, and multilingual text features | CJK support is clearly shipped, but the parity boundary between core multilingual support and engine-specific CJK combinations is still ambiguous. | Export evidence shows engine- and package-specific constraints, especially for mixed-language and RTL interactions. | `.ref/lyx/autotests/export/latex/CJK/`, `.ref/lyx/lib/examples/Language_Support/`, `.ref/lyx/lib/languages` | blocking | Split core multilingual support from engine-specific CJK combinations in later parity planning. |
| oq-flex-inset-granularity | templates, layouts, and customization surfaces | Layout-defined Flex insets may be too numerous to model one row each without making the matrix unreadable. | Layout evidence shows many semantically distinct Flex types, but not all appear to require distinct parity treatment. | `.ref/lyx/lib/layouts/*.layout` | blocking | Keep one primary row for layout-defined custom semantic insets and add subtype-specific notes or supporting rows where parity implications materially differ. |
| oq-module-classification | templates, layouts, and customization surfaces | Specialized shipped modules such as LilyPond, Knitr/Sweave, Braille, and PDF forms need firmer parity classification boundaries. | They are shipped and user-visible, but some are highly specialized or toolchain-dependent. | `.ref/lyx/lib/examples/Modules/`, `.ref/lyx/lib/templates/`, `.ref/lyx/lib/doc/Additional.lyx` | blocking | Confirm whether each module should remain adapted parity, deferred parity, or non-goal in follow-on specs. |
| oq-table-multispan-behavior | tables | Full table multispan and nested-table behavior needs deeper proof for exact export and AST modeling. | Export evidence confirms table complexity, but exact rowspan/colspan and nesting semantics are not yet fully derived. | `.ref/lyx/autotests/export/`, `.ref/lyx/src/insets/InsetTabular.cpp` | blocking | Deep-dive `InsetTabular.cpp` and export fixtures to define canonical table semantics for later AST work. |
| oq-citation-engine-selection | citations and bibliography | Citation engine switching and the exact scope of natbib vs. biblatex behavior remain partially unresolved. | Runtime and documentation evidence confirm multiple engines, but the precise document-level selection and output differences still need tighter localization. | `.ref/lyx/lib/doc/LaTeXConfig.lyx`, `.ref/lyx/src/BiblioInfo.cpp`, `.ref/lyx/src/CiteEnginesList.cpp` | non-blocking | Capture engine-specific cite-command differences in the later AST and LaTeX parity specs. |
| oq-export-format-matrix | export and rendering behavior | The full shipped export format matrix and its constraints are not yet reduced to a canonical table. | Export tests confirm LaTeX, DocBook, XHTML, and conversion coverage, but not a single canonical list of formats, prerequisites, and caveats. | `.ref/lyx/autotests/export/`, `.ref/lyx/src/Exporter.cpp`, `.ref/lyx/src/Converter.cpp` | non-blocking | Produce a later export-format appendix once converter definitions are enumerated. |
| oq-error-recovery-policy | export and rendering behavior | Export parity will need a clear fail-fast vs. graceful-degradation policy for malformed structures and missing packages. | Tests show robustness pressure, but the intended user-facing policy is not explicit in the current evidence set. | `.ref/lyx/autotests/export/latex/lyxbugs-resolved/`, `.ref/lyx/autotests/checklatexexports/` | blocking | Turn this into an explicit downstream design decision in the LaTeX/live-render specification. |
| oq-paste-special-detection | editing workflow and user-facing control surfaces | Paste-special format detection is clearly shipped, but the exact detection and conversion pathway is not yet localized. | Runtime evidence shows multiple paste-special variants, but not the full format-detection logic. | `.ref/lyx/lib/ui/stdmenus.inc`, `.ref/lyx/src/` | non-blocking | Localize clipboard format handlers if paste-special becomes a core parity focus. |
| oq-master-document-export-semantics | document classes and document settings | Master-document linking is shipped, but parent/child export semantics and metadata precedence need deeper proof. | Templates and runtime evidence confirm the capability, but exact output behavior is not fully documented in the current pass. | `.ref/lyx/lib/templates/Books/`, `.ref/lyx/src/Buffer.cpp`, `.ref/lyx/src/insets/InsetInclude*` | non-blocking | Localize include/master-document export behavior if large-project parity becomes foundational. |
| oq-version-control-scope | editing workflow and user-facing control surfaces | Version-control integration is clearly user-visible but depends on optional backends and local environment support. | Menu evidence exists, but the supported backend matrix is not fully derived from the bounded source pass. | `.ref/lyx/lib/ui/stdmenus.inc`, `.ref/lyx/src/` | non-blocking | Treat VCS integration as deferred parity until backend scope is enumerated. |
| oq-preview-render-strategy | export and rendering behavior | Preview generation uses shipped scripts, but fallback renderer strategy and determinism guarantees remain partially unclear. | Runtime evidence confirms preview tooling, yet exact fallback rules and reproducibility constraints need more focused analysis. | `.ref/lyx/lib/scripts/lyxpreview2bitmap.py`, `.ref/lyx/lib/scripts/lyxpreview_tools.py` | non-blocking | Capture preview renderer rules in the downstream LaTeX/live-render spec. |
